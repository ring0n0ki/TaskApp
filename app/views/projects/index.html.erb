<h1>Projects</h1>

<%= link_to "New Project", new_project_path %>

<ul>
  <% @projects.each do |p| %>
  <li>
    <%= p.id %> : <%= link_to p.title, project_path(p.id) %>
    <%= link_to "【Edit】", edit_project_path(p.id) %>
  </li>
  <% end %>
</ul>
